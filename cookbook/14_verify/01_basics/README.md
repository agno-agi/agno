# 01_basics

Core loop: a callable verifier as the gate, and the unverified outcome.

## Files

- `verify_done.py`: callable verifier on done, the file-must-exist gate, with a visible continuation.
- `unverified.py`: an impossible check; the unverified outcome and what a caller reads from the attempts.
