# 03_scorer

ScorerVerifier: the shipped agno.scorer surface as a completion gate.

## Files

- `judge_gate.py`: `ScorerVerifier(JudgeScorer(mode="numeric", threshold=8))`; the judge owns the pass rule.
