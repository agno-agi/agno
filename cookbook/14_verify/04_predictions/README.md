# 04_predictions

verified_tool: a tool call that carries a falsifiable prediction.

## Files

- `verified_tool.py`: a counter tool with a hidden cap; a wrong prediction prefixes a divergence block and forces a replan.
