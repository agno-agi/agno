# 05_fingerprints

Fingerprints: the no-op detector.

## Files

- `noop_guard.py`: `GitWorktreeFingerprint` + `stop_on_noop=True`; an agent that claims done without changing the worktree stops after one attempt.
