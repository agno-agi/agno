# Kenari Tests

### basic.py

**Status:** PASS

**Description:** Basic Kenari model cookbook covering sync, streaming, async, and async streaming agent responses.

**Result:** Ran locally against the live API with `KENARI_API_KEY` set (model `deepseek-v4-flash`). All four response modes completed and produced streamed/completed responses.

---
