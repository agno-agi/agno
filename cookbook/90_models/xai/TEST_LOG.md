# TEST_LOG

### oauth_device_login.py

**Status:** NOT RUN

**Description:** SuperGrok device-flow sign-in followed by one agent response in
each syntax (model class and `xai-responses:` string). Requires an interactive
browser approval with a SuperGrok subscription plus `XAI_TOKEN_ENCRYPTION_KEY`,
so it cannot run unattended. The token manager and provider paths it exercises
are covered by the unit suite at `libs/agno/tests/unit/models/xai_model/`.

**Result:** Pending a manual run with a SuperGrok account.

---
