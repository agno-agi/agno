# Export a report

All Acme Reports plans include CSV export. Open a saved report, choose the project
and date filters, then select Export > CSV. The export contains only rows matching
the active filters. You must have access to the report's workspace.

CSV exports include the column headers and use UTF-8 encoding. They do not include
charts. For recurring delivery, see [Scheduled reports](schedules.md).
