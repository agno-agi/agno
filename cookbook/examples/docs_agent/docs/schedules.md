# Scheduled reports

Scheduled reports require the Team plan and a workspace admin. Open a saved report,
select Schedule, choose daily or weekly delivery, add workspace recipients, and
save. Delivery uses the filters saved on the report.

Only workspace admins can create, change, or disable schedules. A schedule emails
a link to the report; it does not attach a CSV file. Monthly delivery and external
email recipients are not supported.
