[project]
name = "second-brain-example"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["agno[os,sqlite,mcp]==3.0.8", "openai", "cryptography"]

[dependency-groups]
dev = ["pytest", "pytest-asyncio"]
