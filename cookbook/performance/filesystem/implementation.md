# Implementation status

- [x] Independent benchmark directory; existing source and dirty work preserved.
- [x] Deterministic shared corpus and explicit backend adapters.
- [x] Direct operations, raw timing samples, process RSS, and persistence checks.
- [x] Native agent tool-loop measurements with a mocked model.
- [x] Controlled real-model accuracy and cross-session file-memory runner.
- [x] Reproducible package snapshots and result reporting.
- [ ] Complete real-model run after a working provider credential is configured.

This is measurement code, not a filesystem behavior change.
