"""AWS Claude model utilities - V1 compatibility stub."""

from typing import Any, List


def format_messages(messages: List[Any]) -> List[Any]:
    """Format messages for AWS Claude model.

    V1 stub that passes through messages unchanged.
    """
    return messages
